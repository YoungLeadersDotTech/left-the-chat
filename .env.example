# Optional. Kept server-side by the Vite development server.
# When omitted, the app uses its built-in clearly labelled mock model.
OPENAI_API_KEY=

# Optional OpenAI-compatible model name.
OPENAI_MODEL=gpt-4o-mini
